import * as Styled from "./Header.style";

const Header = () => {
  return (
    <Styled.Container>
      <Styled.Nav>
        <Styled.Title>관리자 페이지</Styled.Title>
      </Styled.Nav>
    </Styled.Container>
  );
};

export default Header;
